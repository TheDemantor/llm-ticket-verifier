{
  "info": {
    "name": "DQMS API Testing",
    "description": "Postman collection for testing DQMS (Data Quality Management System) API endpoints",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Health Check",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{base_url}}/health",
          "host": [
            "{{base_url}}"
          ],
          "path": [
            "health"
          ]
        },
        "description": "Health check endpoint to verify server and database connectivity"
      },
      "response": []
    },
    {
      "name": "Chat - Start Session",
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "if (pm.response.code === 200) {",
              "    var jsonData = pm.response.json();",
              "    pm.environment.set('session_id', jsonData.session_id);",
              "    pm.environment.set('problem_id', jsonData.problem_id);",
              "    console.log('Session ID saved:', jsonData.session_id);",
              "    console.log('Problem ID saved:', jsonData.problem_id);",
              "}"
            ]
          }
        }
      ],
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\n  \"user_id\": \"user123\",\n  \"problem_description\": \"Database connection is timing out when running complex queries. The application hangs after 30 seconds of execution. This happens particularly with queries involving multiple joins across large tables with millions of records.\"\n}"
        },
        "url": {
          "raw": "{{base_url}}/api/chat/start",
          "host": [
            "{{base_url}}"
          ],
          "path": [
            "api",
            "chat",
            "start"
          ]
        },
        "description": "Initiates a new chat session for a problem. Returns session_id and initial AI response."
      },
      "response": []
    },
    {
      "name": "Chat - Send User Solution",
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "exec": [
              "// Ensure we have a session_id from previous request",
              "if (!pm.environment.get('session_id')) {",
              "    console.warn('No session_id found. Run \"Chat - Start Session\" first.');",
              "}"
            ]
          }
        }
      ],
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\n  \"session_id\": \"{{session_id}}\",\n  \"user_message\": \"We optimized the queries by adding proper indexes on the join columns and breaking down the complex query into smaller, more efficient queries. We also implemented query caching for frequently accessed data.\",\n  \"message_type\": \"user_solution\"\n}"
        },
        "url": {
          "raw": "{{base_url}}/api/chat/message",
          "host": [
            "{{base_url}}"
          ],
          "path": [
            "api",
            "chat",
            "message"
          ]
        },
        "description": "Sends a user's solution for analysis. Message type should be 'user_solution'."
      },
      "response": []
    },
    {
      "name": "Chat - Send Follow-up Response",
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "exec": [
              "if (!pm.environment.get('session_id')) {",
              "    console.warn('No session_id found. Run \"Chat - Start Session\" first.');",
              "}"
            ]
          }
        }
      ],
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\n  \"session_id\": \"{{session_id}}\",\n  \"user_message\": \"We implemented connection pooling with a maximum of 50 connections and set the timeout to 60 seconds instead of 30. We also added retry logic with exponential backoff.\",\n  \"message_type\": \"response\"\n}"
        },
        "url": {
          "raw": "{{base_url}}/api/chat/message",
          "host": [
            "{{base_url}}"
          ],
          "path": [
            "api",
            "chat",
            "message"
          ]
        },
        "description": "Sends a response to follow-up questions. Message type should be 'response'."
      },
      "response": []
    },
    {
      "name": "Solutions - Validate Solution",
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "exec": [
              "if (!pm.environment.get('problem_id')) {",
              "    console.warn('No problem_id found. Run \"Chat - Start Session\" first.');",
              "}"
            ]
          }
        }
      ],
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\n  \"user_solution\": \"We optimized the database queries by adding indexes on join columns, breaking down complex queries into smaller ones, implementing query caching, setting up connection pooling with 50 max connections, and increasing timeout to 60 seconds with retry logic.\",\n  \"problem_id\": \"{{problem_id}}\"\n}"
        },
        "url": {
          "raw": "{{base_url}}/api/solutions/validate",
          "host": [
            "{{base_url}}"
          ],
          "path": [
            "api",
            "solutions",
            "validate"
          ]
        },
        "description": "Validates a solution against existing solutions for the problem. Returns match percentage and validation status."
      },
      "response": []
    },
    {
      "name": "Solutions - Save Solution",
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "exec": [
              "if (!pm.environment.get('session_id')) {",
              "    console.warn('No session_id found. Run \"Chat - Start Session\" first.');",
              "}"
            ]
          }
        },
        {
          "listen": "test",
          "script": {
            "exec": [
              "if (pm.response.code === 200) {",
              "    var jsonData = pm.response.json();",
              "    pm.environment.set('solution_id', jsonData.solution_id);",
              "    console.log('Solution ID saved:', jsonData.solution_id);",
              "}"
            ]
          }
        }
      ],
      "request": {
        "method": "POST",
        "header": [
          {
            "key": "Content-Type",
            "value": "application/json"
          }
        ],
        "body": {
          "mode": "raw",
          "raw": "{\n  \"session_id\": \"{{session_id}}\",\n  \"problem_description\": \"Database connection is timing out when running complex queries. The application hangs after 30 seconds of execution. This happens particularly with queries involving multiple joins across large tables with millions of records.\",\n  \"root_cause_analysis\": \"The root cause was inefficient query design with missing indexes on join columns, lack of query optimization, missing connection pooling, and insufficient timeout configuration.\",\n  \"solution_details\": \"1. Added database indexes on all join columns\\n2. Refactored complex queries into smaller, optimized queries\\n3. Implemented query result caching for frequently accessed data\\n4. Set up connection pooling with maximum 50 connections\\n5. Increased timeout from 30 to 60 seconds\\n6. Added retry logic with exponential backoff for transient failures\"\n}"
        },
        "url": {
          "raw": "{{base_url}}/api/solutions/save",
          "host": [
            "{{base_url}}"
          ],
          "path": [
            "api",
            "solutions",
            "save"
          ]
        },
        "description": "Saves a complete solution to the database with problem context and root cause analysis."
      },
      "response": []
    },
    {
      "name": "Solutions - Get Solutions by Problem",
      "event": [
        {
          "listen": "prerequest",
          "script": {
            "exec": [
              "if (!pm.environment.get('problem_id')) {",
              "    console.warn('No problem_id found. Run \"Chat - Start Session\" first.');",
              "}"
            ]
          }
        }
      ],
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{base_url}}/api/solutions/{{problem_id}}",
          "host": [
            "{{base_url}}"
          ],
          "path": [
            "api",
            "solutions",
            "{{problem_id}}"
          ]
        },
        "description": "Retrieves all solutions for a specific problem by problem_id."
      },
      "response": []
    }
  ],
  "variable": [
    {
      "key": "base_url",
      "value": "http://localhost:3000",
      "type": "string",
      "description": "Base URL for the DQMS API server"
    },
    {
      "key": "session_id",
      "value": "",
      "type": "string",
      "description": "Session ID from chat/start endpoint"
    },
    {
      "key": "problem_id",
      "value": "",
      "type": "string",
      "description": "Problem ID from chat/start endpoint"
    },
    {
      "key": "solution_id",
      "value": "",
      "type": "string",
      "description": "Solution ID from solutions/save endpoint"
    }
  ]
}
