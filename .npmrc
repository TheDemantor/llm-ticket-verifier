; npm configuration for production builds

# Disable unnecessary metadata
save-false = true
audit = false
fund = false
update-notifier = false

# Install from cache
prefer-offline = true
cache-min = 999999999

# Strict SSL (disable only for development)
strict-ssl = true

# Registry configuration
registry=https://registry.npmjs.org/
